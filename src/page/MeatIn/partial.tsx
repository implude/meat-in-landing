import { styled } from "#/stitches.config";
import { Hexile, Vexile } from "@/component";

export const Landing = styled(Hexile, {
  "@bp1": {
    flexDirection: "column",
  },
});

export const Feature = styled(Hexile, {
  "@bp1": {
    flexDirection: "column",
  },
});
